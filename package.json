{
  "name": "automation-framework",
  "version": "1.0.0",
  "description": "Cypress E2E",
  "main": "index.js",
 "scripts": {
  "test": "cypress run",
  "open": "cypress open",
  "test:report": "cypress run --reporter cypress-mochawesome-reporter && npx mochawesome-merge cypress/reports/*.json -o cypress/reports/report.json && npx marge cypress/reports/report.json -o cypress/reports/html && open cypress/reports/html/index.html"
}
,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.6.0",
    "@types/cypress": "^1.1.6",
    "@types/mocha": "^10.0.10",
    "cypress": "^13.17.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "typescript": "^5.8.2"
  }
}
